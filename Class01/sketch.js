function setup() { //creates the canvas set up- first create page initially
  createCanvas(400, 400);
}

function draw() { //draw things into sketch
  background(220);
  circle(width/2, height/2, 100);
  circle(width/2-20, height/2-20, 20);
}
